# Optional. When set, resume parsing and ATS optimization use Claude for much
# higher-quality results (accurate section extraction, tailored rewriting).
# Without it, the app falls back to a rule-based parser/optimizer and still
# works end to end.
ANTHROPIC_API_KEY=
